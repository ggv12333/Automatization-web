{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Local package.json schema (minimal)",
  "type": "object",
  "description": "A minimal permissive schema to avoid remote schema resolution in editors.",
  "additionalProperties": true
}
